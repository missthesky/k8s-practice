apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pv001-claim0
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 10Mi
  storageClassName: slow
  volumeName: pv001
  