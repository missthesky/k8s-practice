apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pv002-claim0
spec:
  accessModes:
    - ReadWriteOnce
  VolumeMode: Filesystem
  resources:
    requests:
      storage: 10Mi
  storageClassName: slow
  